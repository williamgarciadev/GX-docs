---
title: "HowTo: Deploy Frontend applications to Docker containers"
source_id: 51104
source_url: https://wiki.genexus.com/commwiki/wiki?51104
genexus_version: "18"
---

# HowTo: Deploy Frontend applications to Docker containers

This article shows you the steps to deploy your Frontend application to a Docker container using Nginx. To do so, you can use the [Application Deployment tool](https://wiki.genexus.com/commwiki/wiki?32092) from inside the [GeneXus IDE](https://wiki.genexus.com/commwiki/wiki?5272) or [Msbuild](https://wiki.genexus.com/commwiki/wiki?41321,,) commands.

### [Deploy using the Application Deployment tool inside GeneXus](#Deploy+using+the+Application+Deployment+tool+inside+GeneXus)

**1.** In GeneXus, go through the "Deploy Application" context menu option after having created the [Deployment Unit object](https://wiki.genexus.com/commwiki/wiki?38886) containing the main objects of your app

`[imagen omitida: wiki id 49880]`

**2.** Select the "Static Front end" option from the Targets combo box.

**3.** From the Properties dialog, select "Docker container."

`[imagen omitida: wiki id 51106]`

**4.** Enter valid values for App Location and Docker Image name properties.

**5.** Finally, press the deployment button.

If you wish, you can just create the deployment package by checking "Only Package." In this case, you will have the package under the Deploy\STATICFRONTEND\<DeploymentUnit>\<TimeStamp>\context directory of the [Knowledge Base](https://wiki.genexus.com/commwiki/wiki?1836).  
The context directory contains the source files of the application (which will be used to build the
[Angular](https://wiki.genexus.com/commwiki/wiki?42539) application when the Docker image is built), plus the Dockerfile and *nginx.conf* files.  
`[imagen omitida: wiki id 51114]`

### [Deploy using MSBuild tasks](#Deploy+using+MSBuild+tasks)

See [HowTo: Deploy Static Front end to Docker using MSBuild Tasks](https://wiki.genexus.com/commwiki/wiki?51115)

### [Requirements](#Requirements)

To build the Docker image, you need [Docker for Windows](https://wiki.genexus.com/commwiki/wiki?39548,,).  
For this type of deployment, switch to "Linux Containers."

**Note:** Unless you select "Only Package," if the docker engine is not installed or is not up and running, the following error is thrown:

```
The command "docker info --format "{{.OSType}}"" exited with code 2.
error : Docker client not found.
```

### [Limitations](#Limitations)

The deployment unit must have only one main object.

### [Important Notes](#Important+Notes)

* The application is built in the Docker image (npm install), so building in GeneXus could be avoided (for example, if you don't have the npm on the build machine).
* The Dockerfile settings such as the Nginx version can be edited directly in the Dockerfile template, which is found in the GeneXus installation under GenExtensions\SmartDevices\Angular\deploy\docker\Templates.
* The same happens with the Nginx configuration file (nginx.conf). Through the StaticServerConfigTemplatePath property of the Create Frontend Package MSBuild (explained here), you can indicate a path to the config you want to use. Another option is to edit the nginx.conf file under GenExtensions\SmartDevices\Angular\deploy\docker\Templates.

### [Considerations](#Considerations)

The services of the app have to be deployed in another step. They can be deployed locally, or to the Cloud (to a serverless architecture also using [Azure serverless](https://wiki.genexus.com/commwiki/wiki?49107) or [AWS serverless](https://wiki.genexus.com/commwiki/wiki?35355)).

Consider that in most cases you have to configure the CORS where the services run. See the following for more details:  
[HowTo: Deploy Angular Frontend applications using serverless backend](https://wiki.genexus.com/commwiki/wiki?49963)

### [Availability](#Availability)

Since [GeneXus 17 Upgrade 11](https://wiki.genexus.com/commwiki/wiki?49972,,)


|  |
| --- |
| **Backlinks** |
| [Toc:Application Deployment tool](https://wiki.genexus.com/commwiki/wiki?32092) | [Toc:Application Deployment tool (GeneXus 18 Upgrade 2)](https://wiki.genexus.com/commwiki/wiki?54334) |
| [How to deploy an app generated by Angular Generator](https://wiki.genexus.com/commwiki/wiki?44771) | [HowTo: Deploy Static Front end to Docker using MSBuild Tasks](https://wiki.genexus.com/commwiki/wiki?51115) |

---
