---
title: "GeneXus Angular Generator"
source_id: 42550
source_url: https://wiki.genexus.com/commwiki/wiki?42550
genexus_version: "18"
---

# GeneXus Angular Generator

Angular (together with [React](https://reactjs.org/) and [Vue.js](https://vuejs.org/)) is a front-end framework that constitutes a solution for developing modern, fast, and highly performant web applications.

Therefore, the GeneXus Angular generator is a front-end generator. The generated objects run on the browser and communicate with the back end on the server to get and send information.

The back end is not generated by the Angular generator; instead, it is generated by one of the existing GeneXus generators, such as the [Java generator](https://wiki.genexus.com/commwiki/wiki?12258) or the [.NET generator](https://wiki.genexus.com/commwiki/wiki?38604).

The GeneXus Angular Generator uses the open-source web application framework [Angular](https://angular.io/docs) to help you build modern applications for the web.

## [Angular Generator architecture](#Angular+Generator+architecture)

The solution has a traditional three-tier architecture with a server side (that is, back end) where the services, business logic, and connection to the database are performed. On the client side (that is, front end), there's the UI presentation and all the necessary components to keep the session state and connect to the server side.  
The front end is generated using Angular (Node.js), and the back end can be generated using Java and .NET generators.

`[imagen omitida: wiki id 42549]`

The front end does the following:

Displays information  
Maintains state locally  
Validates the business logic  
Interacts with the server for exchanging data.

## [What language is generated?](#What+language+is+generated%3F)

The code generated by the Angular Generator is HTML and [TypeScript](https://www.typescriptlang.org/). See [GeneXus Angular Generator](https://wiki.genexus.com/commwiki/wiki?42550) for more information.

## [Sources of the application (the project)](#Sources+of+the+application+%28the+project%29)

Every GeneXus object is generated in a separate directory, where all the sources are grouped. This is under the <Model directory>\mobile\Angular\<main>\src\apps folder.

`[imagen omitida: wiki id 42555]`

The following image is a summary of the generated components for an Angular app:

`[imagen omitida: wiki id 42556]`

### [Panels](#Panels)

For each [Panel object](https://wiki.genexus.com/commwiki/wiki?24829), a [component](https://angular.io/guide/glossary#component) (the fundamental building block of Angular applications) is generated, which consists of three sources:

* \*.component.html: It is the layout of the panel. It is an Angular [template](https://angular.io/guide/template-syntax) that represents the layout of the UI. It is mainly HTML, and it adds elements and attributes that Angular interprets to perform the bindings with the data model and the specified UI events.
* \* .component.ts: It is the particular logic of the panel. It is a [TypeScript](https://www.typescriptlang.org/) program. Events are generated here, and the state that is bound to the UI is handled here.
* \* .sercvice.ts: It is the layer responsible for interacting with the server. It is a TypeScript program where communication with the server is generated. Here, the structures are also generated with the data model that the panel will use.

The output of the project is a package that can be downloaded and executed by a browser.

### [Business Components](#Business+Components)

For each [Business Component](https://wiki.genexus.com/commwiki/wiki?5846), two objects are generated:

* \* .dt.ts: It is the data structure of the BC.
* \* .service.ts: It is the layer responsible for interacting with the server.

### [Styles](#Styles)

The [Theme object](https://wiki.genexus.com/commwiki/wiki?16595) and [Design System Object](https://wiki.genexus.com/commwiki/wiki?47375) are generated as a [SASS](https://sass-lang.com/). The classes are converted to CSS if possible, or else to variables that the controls can use to implement the required functionality.

### [Other objects](#Other+objects)

In addition to the previous objects, other sources are generated in Angular:

* app.module: It is the description of the module. It contains the specification required to assemble the package.
* app.routing: It is the router that manages the state changes related to the navigation. Basically, what it does is that given a navigation route, it determines the component to be displayed.
* app.settings: Addresses and properties configuration of the application (for example, the URL of the services; if it has security or not, etc.)
* app.component: It is the application homepage.

### [UI Controls](#UI+Controls)

The control library is implemented in a separate project independent of the Angular generator.

## [Dissecting Angular Architecture](#Dissecting+Angular+Architecture)

Here is a complete guide for understanding the architecture of the generated application.

* <https://github.com/genexuslabs/DissectingAngularArchitecture>

## [Videos](#Videos)

`[imagen omitida: wiki id 20668]` [Professional Frontends with Angular Generator](https://gx30.gxmeeting.genexus.com/website.session.aspx?LanguageParameter=ENG&SessionToken=professional-frontends-with-angular-generator)


|  |
| --- |
| **Pages** |
| [Off Text property](https://wiki.genexus.com/commwiki/wiki?42232) |

---
